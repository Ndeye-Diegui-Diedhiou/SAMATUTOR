# Example environment variables for samatutor-proxy
# Copy to .env and set real values before running
PORT=3001
# Provide your OpenAI API key here (get from https://platform.openai.com)
OPENAI_API_KEY=sk-your-api-key-here-replace-with-real-key
# In production, restrict CORS_ORIGIN to your domain, e.g. https://example.com
CORS_ORIGIN=*
# Rate limiting (requests per window)
RATE_LIMIT_WINDOW_MS=60000
RATE_LIMIT_MAX=30

# Optional proxy key - set in both server and (ONLY FOR DEV) the frontend to restrict access
# In production, prefer a server-side authentication mechanism (sessions, JWT, etc.)
PROXY_KEY=your_dev_proxy_key_here
